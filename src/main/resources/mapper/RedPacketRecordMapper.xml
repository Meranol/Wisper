<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.wisper_one.RedPacket.mapper.RedPacketRecordMapper">
    <insert id="insert" parameterType="com.example.wisper_one.RedPacket.POJO.PO.RedPacketRecordPO">
        INSERT INTO red_packet_record
            (red_packet_id, user_code, amount, created_at)
        VALUES
            (#{redPacketId}, #{userCode}, #{amount}, #{createdAt})
    </insert>

    <select id="exists" resultType="boolean">
        SELECT CASE WHEN COUNT(*) > 0 THEN TRUE ELSE FALSE END
        FROM red_packet_record
        WHERE red_packet_id = #{redPacketId} AND user_code = #{userCode}
    </select>
</mapper>